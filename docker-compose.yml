services:
  
  blog:
    build: .
    container_name: 'blog'
    ports:
      - '3000:3000'
    volumes:
      - blog_data:/app:Z
    env_file:
      - .env.local
    networks:
      - servers


volumes:
  blog_data:

networks:
  servers:
    external: true